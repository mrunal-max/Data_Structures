# Data_Structures
#include <stdio.h>

int main()
{
    int n;
    scanf("%d",&n);
    int a[n],temp,i,j;
    for(i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    for(int k =0;k<n;k++)
    {
    for(i=0;i<n-1;i++){
        if (a[i]>a[i+1])
        {
            temp =a[i];
            a[i]=a[i+1];
            a[i+1]=temp;
        }
        else
        {
            continue;
        }
        
    }
    }
    for(i=0;i<n;i++){
        printf("%d ",a[i]);
}
}
